-- https://medium.com/@abdelaz9z/best-practices-for-database-design-incorporating-timestamps-and-user-metadata-in-tables-2310527dd677

DROP TABLE EXERCISES;
DROP TABLE CUSTOM_TRAININGS;
DROP TABLE CUSTOM_TRAININGS_COUNTER;
DROP TABLE REVIEWS;
DROP TABLE USERDATA;
DROP TABLE AUTHORITIES;
DROP TABLE USERS;

CREATE TABLE IF NOT EXISTS USERS (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username    VARCHAR(64) NOT NULL UNIQUE,
    password    VARCHAR(256) NOT NULL,
    enabled     INT NOT NULL
);

CREATE TABLE IF NOT EXISTS AUTHORITIES (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username    VARCHAR(64) NOT NULL,
    authority   VARCHAR(64) NOT NULL,
    FOREIGN KEY (username) REFERENCES USERS(username)
);

CREATE TABLE IF NOT EXISTS USERDATA (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username    VARCHAR(64) NOT NULL,
    firstName   VARCHAR(64) NOT NULL,
    lastName    VARCHAR(64) NOT NULL,
    email       VARCHAR(256) NOT NULL,
    birthDate   DATE NOT NULL,
    signupDate  DATE NOT NULL,
    count_training0 BIGINT DEFAULT 0,
    count_training1 BIGINT DEFAULT 0,
    count_training2 BIGINT DEFAULT 0,
    count_training3 BIGINT DEFAULT 0,
    FOREIGN KEY (username) REFERENCES USERS(username)
);

CREATE TABLE IF NOT EXISTS CUSTOM_TRAININGS_COUNTER (
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username     VARCHAR(64) NOT NULL,
    trainingId   BIGINT      NOT NULL,
    count        BIGINT DEFAULT 0 NOT NULL,
    FOREIGN KEY (username) REFERENCES USERS (username)
);

CREATE TABLE IF NOT EXISTS CUSTOM_TRAININGS (
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username VARCHAR(64) NOT NULL,
    name     VARCHAR(32) NOT NULL,
    kcal     DOUBLE NOT NULL,
    FOREIGN KEY (username) REFERENCES USERS (username)
);

CREATE TABLE IF NOT EXISTS EXERCISES (
    id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    exercise_name VARCHAR(32) NOT NULL,
    sets          INT NOT NULL,
    reps          INT NOT NULL,
    training_id   BIGINT NOT NULL,
    FOREIGN KEY (training_id) REFERENCES CUSTOM_TRAININGS (id)
);

CREATE TABLE IF NOT EXISTS REVIEWS (
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username VARCHAR(64) NOT NULL,
    content  VARCHAR(512) NOT NULL
);
